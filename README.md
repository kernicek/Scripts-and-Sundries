# Scripts and Sundries

Assorted standalone scripts, one folder per tool/technology they target. Each
script sits directly in its folder alongside a same-named `.md` describing
what it does and how to run it; a script only gets its own subfolder if it
grows enough companion files to need one.

## What's here

- `Autodesk Fusion/` — scripts that run inside Fusion 360's Python API.
